- source_spec: `docs/bmad/implementation-artifacts/spec-1-2-test-projects-and-rebuild-solution-filter.md`
  summary: Stories 2.1/3.1/4.1/5.1 must remove their test project's Threshold=0 override (and 4.1/5.1 re-add ProjectReference+Include) — enforce in those story specs.
  evidence: TODO markers in test csprojs are the only in-code record; review flagged the obligation as easy to lose when those specs are drafted.
- source_spec: `docs/bmad/implementation-artifacts/spec-3-1-validated-processing-options.md`
  summary: CoreBankDemo.PaymentsAPI/appsettings.json and CoreBankDemo.CoreBankAPI/appsettings.json still set PartitionCount:2 (should be 4, ruling A3) and still contain LockRenewIntervalSeconds keys (dead, ruling A4) — epics 4/5 must fix these when rebuilding those projects' config, not just the C# option types.
  evidence: Story 3.1 review found the C# default/removal is correct but the live config files that will actually be bound at runtime were untouched (out of story 3.1's Configuration/-only scope); those projects are currently unbuilt/off-slnf so it's not an active regression, but must not be forgotten when epics 4/5 rebuild them.
- source_spec: `docs/bmad/implementation-artifacts/spec-3-2-distributed-lock-port-and-dapr-implementation.md`
  summary: `DaprDistributedLockService.ExecuteWithLockAsync` reads `daprClient.Lock(...)`'s `TryLockResponse` via `.Success` only and never disposes it, even though Dapr's own SDK samples use `await using` on the response. Low-confidence finding — the lock is still explicitly released via the paired `Unlock` call either way, so this is very unlikely to be a real double-unlock or leak, but worth a second look if Dapr.Client's `TryLockResponse.DisposeAsync` is ever found to manage more than the lock handle itself.
  evidence: Edge-case-hunter review of story 3.2 flagged it after reflecting on Dapr.Client 1.17.9 and finding `TryLockResponse : IAsyncDisposable`; could not determine from decompilation alone whether skipping disposal has any real consequence beyond the explicit `Unlock` call already made.
- source_spec: `docs/bmad/implementation-artifacts/spec-3-2-distributed-lock-port-and-dapr-implementation.md`
  summary: Ordinary ambient-token cancellation during `daprClient.Lock(...)` (e.g. routine host shutdown while polling for a partition lock) is logged at `LogError` with the same message/severity as a genuine Dapr failure, since it falls into the same generic `catch (Exception ex)` as real errors — no dedicated low-severity path for plain cancellation during acquisition. Left as-is for this story (never-throws/returns-false contract is still satisfied); worth revisiting if it ever pollutes error dashboards in a real deployment, though this is demo code that is never deployed to prod.
  evidence: Both blind-hunter and edge-case-hunter review passes on story 3.2 independently flagged this; deliberately not fixed now to keep the patch scoped to the two confirmed correctness bugs (disposal race, ambient-token passed to cleanup Unlock).
- source_spec: `docs/bmad/implementation-artifacts/spec-3-3-cloudevent-types-and-publisher-port.md`
  summary: `DaprEventPublisher.PublishAsync` applies no validation to `type`/`source`/`subject` — null or empty values flow through unchecked into the CloudEvent metadata dictionary (only `traceParent` gets an emptiness check, per the spec's explicit contract). Left as-is — `IEventPublisher` has no caller yet (dead code until story 3.4 wires DI and epics 4/5 add producers), the spec's stated contract is "thin pass-through, propagates unchanged" not "validates," and this is demo code with no adversarial input path.
  evidence: Both blind-hunter and edge-case-hunter review passes on story 3.3 independently flagged the missing null/empty guard on type/source/subject; revisit if a real caller ever supplies these from unvalidated external input.
- source_spec: `docs/bmad/implementation-artifacts/spec-3-3-cloudevent-types-and-publisher-port.md`
  summary: `CloudEventJsonSnapshotTests`'s hand-built `DaprDefaults` (`PropertyNamingPolicy = CamelCase`) is asserted to mirror the real `DaprClient`'s internal `JsonSerializerOptions` only via a code comment citing a one-time manual reflection check, not an automated assertion against a live `DaprClientBuilder` instance — if a future Dapr.Client bump changes those defaults, the snapshot tests would keep passing with false confidence of byte-for-byte fidelity (AD-12).
  evidence: Blind-hunter flagged this directly; the independent verification-gap pass also could not re-derive the claim via fresh reflection and accepted it only on the comment plus passing-test corroboration — itself confirming the gap exists. Low priority since Dapr.Client is version-pinned via central package management and won't drift silently.
- source_spec: `docs/bmad/implementation-artifacts/spec-3-3-cloudevent-types-and-publisher-port.md`
  summary: `DaprEventPublisher.PublishAsync` is a synchronous (non-`async`) method that returns `daprClient.PublishEventAsync(...)` directly; if the Dapr SDK call ever throws synchronously (before returning a Task) rather than faulting the returned Task, a caller that splits the call from its `await` (rather than awaiting inline) would observe a synchronous throw instead of an awaited-task fault. Diverges from the fully-`async` sibling `DaprDistributedLockService` with no comment explaining why.
  evidence: Blind-hunter flagged it, and noted the existing "exceptions propagate" test only exercises `.ThrowsAsync(...)` (a faulted Task), not a genuine synchronous throw. Low priority since every expected caller pattern (`await eventPublisher.PublishAsync(...)` inline, inside a try) observes identical behavior either way.
- source_spec: `docs/bmad/implementation-artifacts/spec-3-4-service-wiring-defaults.md`
  summary: "**Partially resolved by story 4.1; PaymentsAPI half still outstanding.** `AddServiceDefaults` checks for `DaprClient` *eagerly*, at registration time, unlike `IDistributedLockService`'s *lazy* per-resolution check — calling `AddServiceDefaults()` before `AddDaprClient()` silently prevents `IEventPublisher` from ever registering. Story 4.1's rebuilt `CoreBankDemo.CoreBankAPI/Program.cs` gets this right (`AddDaprClient()` called before `AddServiceDefaults()`), so CoreBankAPI's half of this landmine is now defused. `CoreBankDemo.PaymentsAPI/Program.cs` (epic 5, not yet rebuilt) still has the legacy ordering and still never calls `AddMessagingOutboxProcessingOptions()` — both must be fixed when epic 5 rebuilds it, before it resolves `IEventPublisher`."
  evidence: Both blind-hunter and edge-case-hunter review passes on story 3.4 independently flagged the eager-check/registration-order landmine; blind-hunter additionally grepped and confirmed the *current* call order in both real `Program.cs` files already has `AddServiceDefaults()` before `AddDaprClient()` — this is not speculative, it is the present state of those files. `IEventPublisher` has zero callers today (dead code until epics 4/5), so nothing is broken yet, but the landmine is armed for whichever story adds the first real publish call. Not fixed in story 3.4 itself since `Program.cs` for these services belongs to epics 4/5's own demolish-and-rebuild scope, not story 3.4's Code Map.
- source_spec: `docs/bmad/implementation-artifacts/spec-3-4-service-wiring-defaults.md`
  summary: Two low-probability `ResolveOtlpEndpoint` edge cases outside the frozen I/O matrix, both left as-is (demo code, not realistic Aspire/Jaeger config shapes): (1) a bare hostname with no port and no scheme (e.g. `"jaeger"`) falls to the `http://` normalization branch and gets an *implicit* port 80, inconsistent with `tcp://jaeger` (no port) which explicitly defaults to port 4317 via the tcp-rewrite branch; (2) the new `.Contains("://")` guard added to fix the real `Uri.TryCreate` bug (see this story's Spec Change Log) is a string-heuristic, not scheme validation — a contrived value containing a literal `"://"` later in the string (e.g. in a query string) could still reach the first branch and misparse the same way the original bug did.
  evidence: Edge-case-hunter flagged both independently; neither is reachable from any realistic `JAEGER_OTLP_ENDPOINT` value in this repo's Aspire wiring (always `host:port` or `tcp://host:port` with an explicit port), and neither is in the spec's frozen matrix, so left untested/unfixed rather than gold-plating a demo-only config parser.
- source_spec: `docs/bmad/implementation-artifacts/spec-4-1-domain-model-dbcontext-and-seeding.md`
  summary: "`CoreBankDemo.CoreBankAPI/Program.cs`'s `dbContext.Database.EnsureCreatedAsync()` startup call has no synchronization across concurrent instances (Aspire restart/scale-out) — same category of race as the `DemoAccountSeeder` bug fixed in this story, but harder to fix cheaply (would need a distributed lock for a one-time, idempotent-in-practice schema-creation call) and lower probability (`EnsureCreated` internally checks table existence, narrowing the real race window). Left as-is."
  evidence: Blind-hunter and edge-case-hunter both flagged this alongside the (fixed) seeder race; not fixed since `EnsureCreated`'s own internal existence check already narrows the window considerably compared to the seeder's explicit check-then-insert, and adding a distributed lock around a one-time demo-startup schema call is disproportionate for this codebase.
- source_spec: `docs/bmad/implementation-artifacts/spec-4-5-account-endpoints.md`
  summary: "`CoreBankDemo.CoreBankAPI`'s account endpoints (`POST /api/accounts/validate`, `GET /api/accounts/{accountNumber}`) have no authentication, authorization, or rate limiting anywhere in the service — an unauthenticated caller can enumerate syntactically-valid (15-34 char) account numbers and, for every hit, learn the real account holder's name and balance via `validate` even when the account is inactive (that response shape is spec-mandated byte-for-byte legacy parity). Not fixed — this is inherited, frozen, whole-service behavior (no auth exists anywhere in `CoreBankDemo.CoreBankAPI`, not something story 4.5 introduced), the rebuild's binding constraint is to preserve exact external behavior, and this is demo code never deployed to production."
  evidence: Convergent — blind-hunter and edge-case-hunter independently flagged the same underlying exposure (no-auth account/balance enumeration) on story 4.5's review. Not fixed since adding auth would be a genuine external-behavior change (out of scope for a byte-for-byte rebuild story) and would need a real product/architecture decision, not a code fix.
- source_spec: `docs/bmad/implementation-artifacts/spec-4-5-account-endpoints.md`
  summary: "Bundle of single-lens, low-severity findings from story 4.5's review, all left as-is: (1) `AccountQueryHandler` injects no `ILogger<T>` and logs nothing, unlike its sibling `TransactionIntakeHandler` — no CoreBankAPI-side log entry to correlate against a caller's validate/GetDetails failure. (2) `validate` rejects an all-whitespace `AccountNumber` (its DTO carries `[Required]`, which trims) while `GetDetails`' route parameter (only `[StringLength]`, no `[Required]`) accepts the same input class — both inherited byte-for-byte from legacy, not introduced by this story. (3) Account-number lookups are exact-match and case-sensitive with no trimming (`AccountRepository`, frozen story 4.3) — a real account with incidental whitespace or different case silently reads as not-found rather than erroring. (4) A route value containing `/`, `?`, or `#` produces a bare 404/truncated lookup on `GetDetails` instead of the `validate` endpoint's normal body-bound handling of the same string. (5) `AccountDetailsResponse`'s `DateTimeOffset` conversion (`new DateTimeOffset(dt, TimeSpan.Zero)`) would throw if a row's `CreatedAt`/`UpdatedAt` ever had `DateTimeKind.Local` — currently unreachable since every writer uses `TimeProvider`-sourced UTC values, and the pattern is inherited from `TransactionIntakeHandler`. (6) No rounding/scale normalization on `Balance` in JSON responses. (7) `StringLength`'s length check counts UTF-16 code units, so a surrogate-pair character shifts the 15/34 boundary by one from a 'visible character' count — no realistic account-number charset in this demo would ever hit it. (8) One redundant controller test (`ValidateAccount_returns_200_ok_with_the_handlers_response_when_valid` duplicates `..._even_when_invalid`'s code path)."
  evidence: All single-lens (blind-hunter or edge-case-hunter only, not both) or explicitly by-design/inherited-from-frozen-code; none crossed this project's convergence-across-≥2-lenses bar for a direct fix. The one convergent, actionable finding from this review (no test proves the `[StringLength(34, MinimumLength=15)]` boundary against the real attribute instances) was fixed directly — see spec-4-5's Spec Change Log.
- source_spec: `docs/bmad/implementation-artifacts/spec-4-1-domain-model-dbcontext-and-seeding.md`
  summary: `DemoAccountSeeder.SeedAsync`'s `SaveChangesAsync` call has no `IExecutionStrategy`/retry wrapping, so once real Postgres transient-fault retry policies are in play (`AddNpgsqlDbContext` typically enables `EnableRetryOnFailure`), a bare `SaveChangesAsync` outside `IExecutionStrategy.ExecuteAsync` can throw `InvalidOperationException` under combined retry+transaction usage. Was untestable under the old SQLite store tier; since ADR-016 the PostgreSQL Testcontainers persistence tier could exercise it, but the deferred decision (whether to wrap seeding in `IExecutionStrategy`) is unchanged and still open.
  evidence: Blind-hunter flagged it. Revisit if a later story adds transactional multi-step writes through this same DbContext under Postgres's retry policy (story 4.6's atomic inbox execution is the most likely place this could first bite).
- source_spec: `docs/bmad/implementation-artifacts/spec-4-4-idempotent-transaction-intake.md`
  summary: When a `Completed` inbox row's cached payload fails to deserialize (null/empty/corrupt — the defensive fallback path, now fixed to not crash on corrupt JSON), the response is self-contradictory — HTTP 202 Accepted (`TransactionIntakeOutcome.InFlight`) carrying a body whose `Status` field literally reads `"Completed"`. A client reading only the body could reasonably treat the transaction as done while the status code says "still processing." Not fixed because the frozen spec's outcome model (`Accepted/Replayed/InFlight/TransportFailed`) has no dedicated "corrupt data" outcome, and introducing one would exceed this story's scope; this case is also expected to be unreachable in practice per AD-5 (payload and Completed status are written atomically in one transaction).
  evidence: Both blind-hunter and edge-case-hunter review passes on story 4.4 independently flagged the 202-vs-"Completed" body contradiction. Revisit only if this fallback path is ever observed firing for real (would itself indicate a data-integrity bug elsewhere, e.g. a violation of AD-5's atomicity guarantee).
- source_spec: `docs/bmad/implementation-artifacts/spec-4-4-idempotent-transaction-intake.md`
  summary: Several lower-severity, single-lens findings left as-is (demo code, low probability, or matching pre-existing legacy behavior) — (1) `TransactionId` is embedded unescaped into the `Accepted(...)` response's `Location` header; it is validated only by length (`[StringLength(1,100)]`, matching legacy exactly), not charset, so a value containing `/`, `?`, `#`, or CR/LF could produce a malformed header; (2) `TransactionIntakeHandler`'s log calls never use `logger.BeginScope` with `PartitionId`, despite the conventions skill asking for correlation identifiers in log scopes; (3) `GetTransactionStatus`'s route parameter has no length/format validation unlike the POST-side `TransactionId`; (4) `TransactionStatusResponse` uses naive `DateTime` while the frozen `TransactionResponse` uses `DateTimeOffset` — the same logical timestamps are offset-qualified on one branch of the same endpoint and not on the other; (5) a duplicate against a `Failed` row is reported as `400 BadRequest` (client-fault semantics) even though `Failed` represents a backend transport-retry exhaustion, not a client error; (6) the terminal-failure log line uses `LogInformation` rather than `Warning`/`Error`; (7) no test drives two genuinely concurrent `StoreIfNewAsync` calls against the same key (race-safety is proven at the kernel level in `MessageRepositoryBase`'s own tests, not re-proven per-caller here, matching the precedent set in story 4.1).
  evidence: Blind-hunter review of story 4.4 flagged all seven independently; edge-case-hunter and verification-gap reviews did not raise them, so each is single-lens. Left unfixed per this project's established triage bar (single-lens + low severity → log and defer rather than patch).
- source_spec: `docs/bmad/implementation-artifacts/spec-4-7-event-publishing-processor.md`
  summary: Existing persistent CoreBank PostgreSQL databases require recreation after adding the required MessagingOutboxMessage.EventOccurredAt column.
  evidence: CoreBankDemo uses EnsureCreatedAsync without migrations, and EnsureCreated does not upgrade an existing schema; the persistence tier creates a fresh database per test (ADR-016), so no automated tier verifies an in-place upgrade of an already-deployed database.
- source_spec: `docs/bmad/implementation-artifacts/spec-4-7-event-publishing-processor.md`
  summary: CoreBankAPI has no composition-root test that resolves and starts the real Program.cs hosted-service registrations.
  evidence: Story 4.7 processor tests use a validated ServiceCollection matching production registrations, but deletion or miswiring in Program.cs itself would not fail those tests.
- source_spec: `docs/bmad/implementation-artifacts/spec-4-7-event-publishing-processor.md`
  summary: Sprint tracking still lists completed Story 4.6 as backlog.
  evidence: Story 4.6 is implemented and committed, while sprint-status.yaml retains 4-6-atomic-inbox-execution-with-event-enqueue: backlog; this pre-dates Story 4.7.
- source_spec: `docs/bmad/implementation-artifacts/spec-5-1-payment-store-and-idempotency-key-handling.md`
  summary: Existing persistent Payments PostgreSQL databases must be recreated before the rebuilt schema is used.
  evidence: Story 5.1 replaces required inbox columns and indexes, while the repository mandates EnsureCreatedAsync and forbids migrations; EnsureCreated does not upgrade existing tables.
- source_spec: `docs/bmad/implementation-artifacts/spec-5-1-payment-store-and-idempotency-key-handling.md`
  summary: Replace the temporary OutboxProcessingOptions lock/poll consumer placeholders when Story 5.4 builds the forwarding processor.
  evidence: Story 5.1 consumes PartitionCount only; LockExpirySeconds and PollingIntervalMs remain intentionally unread until the kernel-backed Payments outbox processor exists.

## Deferred from: code review of story-5-1-payment-store-and-idempotency-key-handling (2026-08-30)

- source_spec: `docs/bmad/implementation-artifacts/spec-5-1-payment-store-and-idempotency-key-handling.md`
  summary: Three demo `.http` files (`demo-requests.http`, `payment-idempotency-tests.http`, `CoreBankDemo.PaymentsAPI/CoreBankDemo.http`) still `POST /api/payments`, which this story's demolition deletes.
  evidence: Blind-hunter flagged the dead requests; the endpoint path is frozen and unchanged, so these self-heal once Story 5.2 restores `POST /api/payments` — matches this project's established pattern of transitional `.http` staleness during incremental rebuild (see the two earlier PaymentsAPI `.http`-file entries above).
- source_spec: `docs/bmad/implementation-artifacts/spec-5-1-payment-store-and-idempotency-key-handling.md`
  summary: The deleted legacy `PaymentsController`'s rich `Payment.Received` span tags (`payment.from_account`, `payment.to_account`, `payment.amount`, `outcome`, etc.) have no replacement in the rebuilt handler; only ambient trace-context propagation (`TraceParent`/`TraceState` persistence) survives.
  evidence: Blind-hunter flagged the lost tagging. The `AddServiceDefaults` ActivitySource-list drop itself is correct (no new `ActivitySource` is created anywhere in `PaymentStorageHandler`, confirmed by the explicit "never a second ActivitySource" comment later added to `Program.cs`), but no later story (through 5.6, checked in the current repo) has re-added descriptive per-payment span tags either.
- source_spec: `docs/bmad/implementation-artifacts/spec-5-1-payment-store-and-idempotency-key-handling.md`
  summary: `PaymentRequest.FromAccount == request.ToAccount` (self-transfer) is not rejected anywhere in the payment-storage path.
  evidence: Edge-case-hunter flagged the missing guard. The deleted legacy `PaymentsController` never validated this either (pre-existing, not introduced by this story), and deciding whether/where to reject self-transfers (PaymentsAPI intake vs. CoreBankAPI transaction processing) is a broader business-rule question outside this story's narrow payment-store/idempotency scope.
- source_spec: `docs/bmad/implementation-artifacts/spec-6-2-renewable-redis-distributed-locking.md`
  summary: "`docs/bmad/planning-artifacts/epics.md`'s Story 6.1 acceptance criteria still reads \"Dapr components (pubsub, lockstore, subscription) ... come up healthy,\" even though the Dapr `lockstore` component this story removes no longer exists and ADR-011/ADR-014 replace it with Redis."
  evidence: Blind-hunter flagged the stale text while reviewing story 6.2's diff. Predates this session (already committed before story 6.2's implementation began) and is a documentation-only inconsistency in a different story's acceptance text, not story 6.2's code.
- source_spec: `docs/bmad/implementation-artifacts/spec-6-2-renewable-redis-distributed-locking.md`
  summary: The accepted ADR set (ADR-002 through ADR-014, all committed before this story's implementation began) drops the trailing double-space Markdown "hard line break" convention on the Date/Status/Deciders/Supersedes header lines without adding blank lines between them, collapsing those fields into one run-on paragraph under standard/GFM Markdown rendering.
  evidence: Blind-hunter flagged the rendering regression while reviewing story 6.2's diff (which happened to include the whole ADR set relative to this story's baseline commit). Spans the entire ADR set, not story 6.2's own ADR-011 specifically, and the files were already committed before this session's implementation work — a documentation formatting fix, not a code change.
- source_spec: `docs/bmad/implementation-artifacts/spec-6-2-renewable-redis-distributed-locking.md`
  summary: Two documents describe overlapping "what supersedes the old locking decision" territory without stating which is canonical — ADR-004 gets a note that it is "Superseded in part by ADR-011," while the architecture spine marks its own AD-7 as "[AMENDED BY ADR-011]" for what appears to be the same locking-behavior content.
  evidence: Blind-hunter flagged the ambiguity while reviewing story 6.2's diff. Both documents were already committed before this session's implementation work; resolving which document is the authoritative source for locking behavior going forward is a documentation-consistency decision, not something story 6.2's code can fix.
- source_spec: `docs/bmad/implementation-artifacts/spec-6-2-renewable-redis-distributed-locking.md`
  summary: The real-Redis integration test (`RedisDistributedLockServiceRealRedisTests`) proves lease renewal using hardcoded wall-clock `Task.Delay` margins (3.5s hold vs. a 2s initial expiry, a 2.5s external-contention check) with no clock abstraction, unlike the retired Dapr-adapter tests that used `FakeTimeProvider` for determinism.
  evidence: Blind-hunter flagged the flakiness risk. `DistributedLock.Redis`'s renewal timer is internal library behavior, not application code, so `TimeProvider` injection isn't available here the way it was for the old 5/6 cutoff logic; the test is already isolated from the unit-test gate (`[Trait("Category", "Integration")]`, dynamically skips without a reachable Redis) per the spec's own instruction, limiting the blast radius of any CI flakiness to that separated tier.
- source_spec: `docs/bmad/implementation-artifacts/spec-6-2-renewable-redis-distributed-locking.md`
  summary: "`CoreBankDemo.ServiceDefaults/Extensions.cs`'s `IDistributedLockService` factory calls `sp.GetService<IConnectionMultiplexer>()` without a try/catch; if Aspire's registered `IConnectionMultiplexer` singleton factory throws while connecting (e.g. Redis briefly unreachable at the exact moment of first resolution), that exception would propagate out of DI resolution instead of falling back to `NoOpDistributedLockService`."
  evidence: Edge-case-hunter flagged the gap. Mitigated in the normal `aspire run` startup path by `AppHost.cs`'s `.WaitFor(redis)` on both API resources (Redis is confirmed healthy before either API's `Program.cs` runs), and the spec's own I/O & Edge-Case Matrix only commits to graceful `false`-returning behavior for Redis failures encountered during lock acquire/extend/release, not during initial DI resolution — so this is outside the frozen intent's explicit scope, not a violation of it.
- source_spec: `docs/bmad/implementation-artifacts/spec-restore-redis-development-password-default.md`
  summary: Redis authentication is configured independently in the AppHost parameter and Dapr component YAML, so overriding only `Parameters:redis-password` causes Dapr authentication failures.
  evidence: The AppHost default and both Dapr pub/sub component files currently repeat `myredispassword123`; no shared substitution or consistency test prevents future drift.
- source_spec: `docs/bmad/implementation-artifacts/spec-fix-payments-http-demo-account-mismatch.md`
  summary: Add an automated AppHost smoke test that executes the checked-in PaymentsAPI demo request and verifies outbox completion and duplicate suppression.
  evidence: The corrected request was verified against the real AppHost, but no automated test consumes the `.http` artifact; a future account-fixture mismatch could therefore pass the existing unit and composition suites.
- source_spec: `docs/bmad/implementation-artifacts/spec-fix-payments-http-demo-account-mismatch.md`
  summary: Remove or replace the stale `GET /api/outbox/check-index` request in the PaymentsAPI `.http` file.
  evidence: Repository search finds the route only in `CoreBankDemo.PaymentsAPI/CoreBankDemo.http`; no PaymentsAPI endpoint implements it, so running that request returns no useful diagnostic result.

## Deferred from: code review of story-5-2-payment-intake-endpoint (2026-08-30)

- source_spec: `docs/bmad/implementation-artifacts/spec-5-2-payment-intake-endpoint.md`
  summary: No `[Authorize]`/authentication on the payment-intake endpoint (`POST /api/payments`), which moves money between accounts.
  evidence: Blind-hunter flagged the missing authorization check. Confirmed pre-existing and codebase-wide: `CoreBankDemo.CoreBankAPI/Controllers/TransactionsController.cs` (the story's own cited thin-controller precedent) has no `[Authorize]` attribute either, and no authentication/authorization convention exists anywhere in the codebase yet.
- source_spec: `docs/bmad/implementation-artifacts/spec-5-2-payment-intake-endpoint.md`
  summary: No `[ProducesResponseType]`/`[Produces]`/`[Consumes]` metadata on `PaymentsController.ProcessPayment`, so the `202`/`400` response shapes aren't reflected in OpenAPI/Swagger generation.
  evidence: Blind-hunter flagged the missing metadata. Confirmed pre-existing and codebase-wide: `TransactionsController` (the story's own cited precedent) carries no such attributes either, despite `Swashbuckle.AspNetCore` being referenced in the project.

## Deferred from: code review of story-5-3-contract-generated-kiota-corebank-client (2026-08-30)

- source_spec: `docs/bmad/implementation-artifacts/spec-5-3-contract-generated-kiota-corebank-client.md`
  summary: Two separate `dotnet-tools.json` tool manifests exist at different paths — a pre-existing, non-standard root-level `/dotnet-tools.json` (holding `dotnet-outdated-tool`) and this story's new, standard-location `.config/dotnet-tools.json` (holding `microsoft.openapi.kiota`) — not consolidated.
  evidence: Blind-hunter flagged the split manifests. Confirmed via `git log` that the root-level file predates this commit (last touched at `e38a7b1`) and was already orphaned from standard `dotnet tool restore` resolution (which only walks up looking for `.config/dotnet-tools.json`) before this story's change — pre-existing, not introduced by story 5.3.
- source_spec: `docs/bmad/implementation-artifacts/spec-5-3-contract-generated-kiota-corebank-client.md`
  summary: "`TransactionStatusResponse.ReceivedAt`/`ProcessedAt` are plain `DateTime`, unlike every other CoreBankAPI wire timestamp (`AccountDetailsResponse.CreatedAt/UpdatedAt`, `TransactionResponse.ProcessedAt`, all `DateTimeOffset`); an offset-less serialization could be misinterpreted as local time rather than UTC when parsed into the new `TransactionStatus.ReceivedAt`/`ProcessedAt` (`DateTimeOffset?`) on the Payments side."
  evidence: Blind-hunter flagged the type inconsistency. `TransactionStatusResponse.cs` is unchanged by this diff (pre-existing from story 4.4); `TransactionIntakeHandler.cs:100` does construct the value via `now.UtcDateTime` (Kind=Utc) at creation time, but round-trip Kind-preservation through EF/Postgres isn't verified by this story.
- source_spec: `docs/bmad/implementation-artifacts/spec-5-3-contract-generated-kiota-corebank-client.md`
  summary: "`LastResponseStatusHandler`'s single static `AsyncLocal<StatusCapture?>` slot would cross-contaminate if a future caller ever issued two `ICoreBankApiClient` calls concurrently from the same DI scope before either awaited far enough to send its request — the second `BeginCapture()` would overwrite the first call's capture slot."
  evidence: Flagged independently by both blind-hunter and the verification-gap reviewer. Not reachable today: the verification-gap reviewer confirmed (via a real full-suite test run, 146/146 passing, 100% line / 98.91% branch coverage) that every current caller — all tests in this diff, and story 5.4's `HttpForwardOutboxDeliveryStrategy` — invokes `ICoreBankApiClient` sequentially, never concurrently, from one scope.
- source_spec: `docs/bmad/implementation-artifacts/spec-fix-dapr-replica-integration.md`
  summary: Reconcile Story 6.3's reset owner so the AppHost initializer, not k6 setup, owns reset-before-release ordering.
  evidence: ADR-014 assigns ordering to an explicit AppHost initializer while the current Story 6.3 spec describes k6's `/reset` call as the initializer.
- source_spec: `docs/bmad/implementation-artifacts/spec-fix-dapr-replica-integration.md`
  summary: Make the Story 6.3 Redis release protocol subscribe before checking the generation marker and atomically advance/publish that generation.
  evidence: Checking the marker before subscribing permits a release between those operations to be missed, leaving a late replica blocked.
- source_spec: `docs/bmad/implementation-artifacts/spec-fix-dapr-replica-integration.md`
  summary: Define pause/reset/release semantics before allowing a second `/reset` after one-way processor gates have opened.
  evidence: The current idempotent-reset matrix permits a second reset while processors remain active and can read or write the stores being truncated.
- source_spec: `docs/bmad/implementation-artifacts/spec-fix-dapr-replica-integration.md`
  summary: Require acknowledgements from every expected replica before the load initializer allows k6 to start.
  evidence: A successful Redis publish proves delivery was attempted, not that every replica opened its local processor gate.
- source_spec: `docs/bmad/implementation-artifacts/spec-6-3-replicated-local-api-topology.md`
  summary: Run both completed AppHosts and retain live 2x2 topology, reset-before-processing, stable-ingress, and shared-Dapr evidence.
  evidence: Builds and executable PostgreSQL/Redis acceptance tests pass, but the execution sandbox denied Aspire DCP loopback traffic; the user accepted closure and retained this live run as a human follow-up.
- source_spec: `docs/bmad/implementation-artifacts/spec-6-3-replicated-local-api-topology.md`
  summary: Record a failed HTTP delivery attempt when `ProcessTransactionAsync` throws a non-cancellation transport exception.
  evidence: Story 6.5 records delivery outcomes only after `ProcessTransactionAsync` returns, so thrown transport attempts currently emit no failed delivery metric.
- source_spec: `docs/bmad/implementation-artifacts/spec-6-3-replicated-local-api-topology.md`
  summary: Count tracked CoreBank outbox additions when an enqueuer adds a row and then throws before returning.
  evidence: `TransactionExecutionHandler` increments `enqueuedEventCount` only after each enqueuer returns, so rollback metrics undercount an add-then-throw path.
- source_spec: `docs/bmad/implementation-artifacts/spec-6-3-replicated-local-api-topology.md`
  summary: Assert production PostgreSQL repository transition return values for applied and already-terminal completion/failure paths.
  evidence: Existing persistence tests verify durable state but discard `MessageTransitionOutcome`, while processor tests supply mocked outcomes.
- source_spec: `docs/bmad/implementation-artifacts/spec-6-3-replicated-local-api-topology.md`
  summary: Prevent incompatible `BusinessMetrics.StoreName` and `StoreKind` combinations.
  evidence: The typed metrics API accepts both enums independently and can emit impossible store-name/store-kind series if a caller pairs them incorrectly.

## Deferred from: story 7.1 (assertion API realignment)

- source_spec: `docs/bmad/implementation-artifacts/spec-7-1-assertion-api-realignment.md`
  summary: Fix `CoreBankDemo.LoadTestSupport/Properties/launchSettings.json`'s `applicationUrl` from `http://localhost:5180` to `5181` to match Aspire's AppHost wiring.
  evidence: Explicitly out of scope per this story's Boundaries & Constraints ("Never" list); confirmed still present by reading the file during story 7.1's implementation.
- source_spec: `docs/bmad/implementation-artifacts/spec-7-1-assertion-api-realignment.md`
  summary: Extend `NoFailedMessages` in `LoadTestAssertionService.GetResultsAsync` to check `paymentsDb.OutboxMessages`, `paymentsDb.InboxMessages`, and `coreBankDb.MessagingOutboxMessages`, not only `coreBankDb.InboxMessages`.
  evidence: Blind-hunter review of the story 7.1 diff found `failedCount` is computed exclusively from `coreBankDb.InboxMessages` — a permanently `Failed` row in the other three stores is invisible to the assertion suite. Pre-existing (unchanged by this story's diff, matches `main`), but it undermines invariant 4 (terminal-state completeness) that Story 7.3's full acceptance run depends on; recommend fixing before or alongside Story 7.3.
- source_spec: `docs/bmad/implementation-artifacts/spec-7-1-assertion-api-realignment.md`
  summary: Confirm whether `LoadTestAssertionService`'s duplicate-key `GroupBy` check in `GetResultsAsync` is intentional defense-in-depth or removable dead code, and document the decision.
  evidence: Blind-hunter review found `coreBankDb.InboxMessages.IdempotencyKey` carries a unique index (confirmed by the new integration test's own setup), so the `GroupBy(...).Where(g => g.Count() > 1)` duplicate check can structurally never return a result against the real schema. Pre-existing, not introduced by this story.
- source_spec: `docs/bmad/implementation-artifacts/spec-7-1-assertion-api-realignment.md`
  summary: Decide whether `AssertionResult.Debug.CompletedTransactions` needs a row cap (matching `InboxEndpoints`/`OutboxEndpoints`'s existing 50-row cap) and apply it consistently to both REST and MCP.
  evidence: Edge-case-hunter review found the shared `AssertionResult` now exposes the full completed-transaction list to `get_assertion_results` (MCP) with no size bound, unlike this story's own capped inspection endpoints. Needs a product decision — REST's failure-diagnostic use may need the full list — rather than a blind cap, so left for follow-up.

## Deferred from: story 7.3 (k6 run and first full acceptance gate)

- source_spec: `docs/bmad/implementation-artifacts/spec-7-3-k6-run-and-first-full-acceptance-gate.md`
  summary: `docs/bmad/planning-artifacts/epics.md` still states the frozen Story 3.3 acceptance criterion for `IEventPublisher.PublishAsync` with the old 5-parameter signature; ADR-017 supersedes it with a 7-parameter signature but the planning artifact isn't annotated to point there.
  evidence: Blind-hunter review confirmed the stale text at `epics.md:260`; this story's frozen `<frozen-after-approval>` boundaries and Code Map don't cover planning-artifact edits, so left for a documentation pass rather than a blind change to an upstream planning doc.
- source_spec: `docs/bmad/implementation-artifacts/spec-7-3-k6-run-and-first-full-acceptance-gate.md`
  summary: `StageCardinalityCheck` (and the pre-existing `expectedUniqueProcessed` check it follows the same convention as) reports `Passed=true` when `expectedUnique` is omitted from `/assert/results`, i.e. it fails open rather than closed.
  evidence: Edge-case-hunter and blind-hunter both flagged this. Currently unreachable in practice — the k6 script and the MCP `get_assertion_results` tool schema both require `expectedUnique` — but it violates the story's own stated fail-closed principle at the REST boundary. Fixing it consistently (for both the new and the pre-existing check) is a deliberate design decision, not a one-line patch.
- source_spec: `docs/bmad/implementation-artifacts/spec-7-3-k6-run-and-first-full-acceptance-gate.md`
  summary: `tests/CoreBankDemo.LoadTestSupport.Tests/K6ScriptContractTests.cs` verifies `k6/script.js`'s fail-closed behavior purely via string-containment on the raw file text, not by actually executing the script under k6 or any JS runtime.
  evidence: Both the verification-gap and blind-hunter reviews independently flagged this as the same gap — a check literal moved into a comment, or a `check(...)` call relocated to an unreachable branch, would still pass these tests. The verification-gap reviewer explicitly noted the repo has no JS-execution test infrastructure to close this gap without introducing new tooling, so it's left as a known limitation; the current acceptance evidence is grounded in an actual live k6 run (`docs/bmad/implementation-artifacts/7-3-acceptance-evidence.md`), not these tests alone.
- source_spec: `docs/bmad/implementation-artifacts/spec-7-3-k6-run-and-first-full-acceptance-gate.md`
  summary: `tests/CoreBankDemo.Persistence.IntegrationTests/CoreBankApi/ReplicatedCoreBankInboxProcessorTests.cs` duplicates nearly all of `ReplicatedCoreBankOutboxProcessorTests`'s coordination scaffolding (`CoordinatedLockService`, `TaskCompletionSource` wiring, replica bookkeeping) instead of sharing a base/helper.
  evidence: Blind-hunter review found the duplication; both test classes pass and are load-bearing for the story's ordering/exclusivity proof, so refactoring passing test scaffolding was left out of this pass to avoid unnecessary risk to already-verified evidence.
- source_spec: `docs/bmad/implementation-artifacts/spec-7-3-k6-run-and-first-full-acceptance-gate.md`
  summary: `k6/script.js`'s `'retry accepted (202) or conflict (409/202)'` check only ever asserts `r.status === 202` — its own name promises tolerance for `409` that the code doesn't provide.
  evidence: Blind-hunter review found this pre-existing check (unchanged by this story's diff) is now more consequential because this story's new blanket `checks: ['rate==1']` threshold makes every check gate the run's exit code. `PaymentsController.ProcessPayment` never returns 409 today, so it's latent, not currently failing.
- source_spec: `docs/bmad/implementation-artifacts/spec-7-3-k6-run-and-first-full-acceptance-gate.md`
  summary: `k6/script.js`'s final assertion block reads `r.checks.stageCardinality.passed`/`r.checks.canonicalAccountSet.passed` without first checking those fields exist on the parsed response.
  evidence: Edge-case-hunter review found this would throw a `TypeError` (rather than fail a named check) if `/assert/results` ever omitted these new fields, e.g. under REST/MCP client-server version skew. Consistent with every other check in that same block, which has the same unguarded-property-access pattern; not a regression specific to this story's new fields.

## Deferred from: code review of spec-add-instant-rail-load-coverage.md (2026-09-03)

- source_spec: `docs/bmad/implementation-artifacts/spec-add-instant-rail-load-coverage.md`
  summary: `k6/script.js`'s single shared `http_req_duration{type:payment}` threshold (`p(95)<2000ms`) now covers both standard and the new ~20% instant-scheme traffic, even though the instant rail can legitimately block for up to `AttemptTimeoutMilliseconds`/`BudgetMilliseconds` (2500ms/9000ms in `PaymentsAPI/appsettings.json`) — a run could fail on latency grounds unrelated to correctness if CoreBank is ever slower than usual.
  evidence: Blind-hunter review flagged the shared threshold. Not fixed now since it didn't manifest in this story's live run (all thresholds green) and choosing a new per-scheme threshold value or k6 request tag is a design decision, not a mechanical fix — left for whoever builds the DevProxy latency-injection follow-up (also tracked in this file), which will need to reason about instant-rail latency thresholds anyway.

## Deferred from: three-layer adversarial review of story 7.4 (2026-09-04)

- source_spec: `docs/bmad/implementation-artifacts/spec-7-4-reusable-terminal-demo-operator-console.md`
  summary: "`POST /run-evidence/inline-settlement` (`CoreBankDemo.LoadTestSupport/Endpoints/RunEvidenceEndpoints.cs`) accepts k6's self-report of an inline instant-payment settlement with no server-side corroboration against the real `Completed` row in `paymentsDb`/`coreBankDb` — a k6 script bug or a deliberately falsified `IdempotencyKey` would inflate `LoadRunEvidenceState.InlineSettlementCount` and make `InlineInstantSettlement` (one of the five acceptance invariants) pass without a genuinely-completed inline payment ever existing."
  evidence: Confirmed by reading `RunEvidenceEndpoints.MapRunEvidenceEndpoints` and `LoadTestAssertionService`/`LoadTestAssertionCalculator` during the three-layer adversarial review (blind-hunter, edge-case-hunter, verification-gap) of story 7.4's uncommitted diff — the handler only validates the key's shape (`load-test-` prefix, non-empty) before calling `state.RecordInlineSettlement(evidence.IdempotencyKey)`; nothing queries `paymentsDb`/`coreBankDb` to confirm that key actually reached `Completed` via the instant rail. Not fixed in this round (out of the ten confirmed-and-scoped patches) — closing it would mean querying the durable stores from this endpoint, a design change to the evidence-recording contract rather than a bug fix.
- source_spec: `docs/bmad/implementation-artifacts/spec-7-4-reusable-terminal-demo-operator-console.md`
  summary: "`DoctorRunner.RunAsync`'s per-profile `{profile} topology` check (used by the `--doctor` CLI output path) reports `Ok` whenever `discovered.IsReachable && snapshot is null && (portsFree || readyActiveProfile is not null)` — but when `portsFree` is `false` and `readyActiveProfile is not null` (a required port is held by the *other* profile), that profile's `CanStart` is still correctly `false` in `ProfilePreflightResult`/`DoctorReport.CanStart`, yet the `--doctor` CLI's check list prints `Ok` for its topology row, understating a real cross-profile port-contention block. The TUI itself is unaffected: `OperatorConsoleController.StartAsync`/`MainWindow` gate the Start button and the Start command on `preflight.CanStart(profile)` directly, never on this check's `Passed` flag."
  evidence: Confirmed by reading `CoreBankDemo.DemoRunner/Application/Doctor/DoctorRunner.cs:116-121` during the same review — the `snapshot switch` arm `null when discovered.IsReachable && (portsFree || readyActiveProfile is not null) => DoctorCheckResult.Ok(...)` accepts the `readyActiveProfile is not null` disjunct as sufficient for `Ok` even when `portsFree` is `false`, while `ProfilePreflightResult.CanStart` (line 106) requires `portsFree` unconditionally. Not fixed in this round (out of scope for the ten confirmed patches) — the CLI-only nature and the TUI's independent, correct `CanStart` gate keep this from being a live operator-facing bug today.
- source_spec: `docs/bmad/implementation-artifacts/spec-7-4-reusable-terminal-demo-operator-console.md`
  summary: "`OperatorConsoleController`'s private `ResourceWaitResult.Partial` (and the sibling `ResourceDispatchStatus.Partial`) both surface to the operator as the same `\"Partial mutation...\"` evidence/status text, but they mean two different things: `ResourceDispatchStatus.Partial` (`AspireCliAdapter.cs:202`) means a multi-instance resource command was accepted by some instances and rejected by others; `ResourceWaitResult.Partial = true` (`OperatorConsoleController.cs:1114-1121`) means the command fully dispatched but the resource then degraded/failed while `WaitForResourceAsync` was polling for confirmation. An operator reading only the evidence text (`\"Partial mutation: ... Refresh is required...\"`) cannot tell a rejected-by-some-instances dispatch apart from a degraded-after-full-dispatch resource."
  evidence: Confirmed by reading `OperatorConsoleController.cs:577-620` and `AspireCliAdapter.cs:190-205` during the same review — both code paths independently produce operator-facing text built from the literal `\"Partial mutation\"` phrase, and neither includes which of the two underlying conditions triggered it. Not fixed in this round (out of scope for the ten confirmed patches) — distinguishing the two in the operator-facing text is a UX/evidence-shape decision, not a correctness bug in either path.
- source_spec: `docs/bmad/implementation-artifacts/spec-demorunner-fault-injection-workspace.md`
  summary: "The LoadTests Dev Proxy profile and `k6/script.js` are already mutually incompatible, independent of the Faults workspace: `k6/script.js:148-153` throws unless a fresh `scheme=instant` payment settles inline within the 9000ms `BudgetMilliseconds`, while `CoreBankDemo.LoadTests/devproxy/config/devproxyrc-latency.json` deliberately injects 9500-12000ms on `/api/Transactions/*` — so arming Dev Proxy on the LoadTests topology aborts the k6 run in `setup()` before any assertion executes."
  evidence: Confirmed by reading `k6/script.js:148-153` against `CoreBankDemo.LoadTests/AppHost.cs:66-72` (whose own comment states the band is tuned to overrun the instant rail's budget) during the three-layer review of this spec's diff. This predates the Faults workspace and is very likely why the profile ships `Features:UseDevProxy: false` and the AppHost comment says "never part of the /run-load-tests gate." Not fixed here — reconciling the setup probe with a deliberately-overrunning latency band is a load-suite decision, not a console change.
- source_spec: `docs/bmad/implementation-artifacts/spec-demorunner-fault-injection-workspace.md`
  summary: "The load-test assertion suite cannot survive injected errors or rate limiting: at minimum `NoFailedMessages`, `NoPendingMessages`, `ExpectedUniqueProcessed`, `AllSubmittedProcessed`, `PerKeyOrdering` and `StageCardinality` fail deterministically once the LoadTests profile injects 503/429/500 or throttles, while `BalanceConservation` and `BalancesCorrect` keep passing vacuously — so a chaos-enabled load run needs a separate fault-tolerant assertion mode rather than the current exact-cardinality/zero-Failed suite."
  evidence: Confirmed by reading `CoreBankDemo.LoadTestSupport/Services/LoadTestAssertionService.cs:420-523` and `MessageRepositoryBase.ApplyFailureTransition:387-393` during the same review. `PerKeyOrdering` is the most structural: it requires every row to carry a `ProcessedAt` (a terminally-`Failed` row never gets one) and asserts FIFO by `EnqueuedAt`->`ProcessedAt`, but retry-after-error *is* a reordering mechanism. `BalancesCorrect` replays expectations from the `Completed` rows themselves, so it cannot detect a dropped payment. Out of scope for the console feature, which only warns before Run when faults are in force.
- source_spec: `docs/bmad/implementation-artifacts/spec-demorunner-fault-injection-workspace.md`
  summary: "`tests/` and `Tests/` resolve to the same directory on this case-insensitive volume, and `dotnet test tests/CoreBankDemo.DemoRunner.Tests/` fails with `MSBUILD : error MSB1050: Specify which project or solution file to use because the folder ... contains more than one project or solution file` — every invocation must pass the `.csproj` path explicitly."
  evidence: Confirmed during verification of this spec's implementation: `ls -di Tests tests` reports the same inode (29188347), `git config core.ignorecase` is `true`, and `git ls-files` shows 149 files under lowercase `tests/` and none under `Tests/`. Pre-existing and unrelated to the Faults workspace, but it silently breaks the most obvious form of the documented test command.
- source_spec: `docs/bmad/implementation-artifacts/spec-demorunner-fault-injection-workspace.md`
  summary: "`DESIGN.md`'s Do's and Don'ts table contradicts itself on `accent-teal`: row 307 still reads \"Use `accent-teal` only for the primary action and focus ring\" while row 316 states the three roles the Faults pass established (primary action, focus ring, lock-exempt/faults-in-force). Row 316 is the live rule; row 307 is stale."
  evidence: Confirmed by reading `docs/bmad/planning-artifacts/ux-designs/ux-CoreBankDemo-2026-09-03/DESIGN.md:307` against `:316` and the Colors section at `:235-236`, which explicitly enumerates the three roles. Not corrected during the build because the spines are a finalized upstream contract owned by `bmad-ux`, not an implementation artifact.
- source_spec: `docs/bmad/implementation-artifacts/spec-demorunner-fault-injection-workspace.md`
  summary: "Two statements in the finalized `ux-CoreBankDemo-2026-09-03` spines are now factually wrong and need a bmad-ux amendment. (1) `EXPERIENCE.md:28` assumes Dev Proxy's watched-file reload is the mechanism by which levels change on a running proxy — Dev Proxy 3.2.0 cannot reload its own configuration at all, so the console restarts the `devproxy` Aspire resource after each write instead (ADR-019). (2) `EXPERIENCE.md:119` justifies the Faults workspace's exemption from the single-action-in-flight lock by claiming a fault change \"cannot corrupt an in-flight payment, only degrade the conditions that payment meets\" — with a restart in the apply path, a call in flight *through the proxy* can now fail outright rather than merely slow down. The exemption itself still holds (raising a level mid-burst is the demonstration), but its stated justification no longer does, and the cost belongs in the spine's copy as well as in the workspace's."
  evidence: "Measured against a real Dev Proxy 3.2.0 during review loop 2 of this spec: a generated config is honoured at process start (three latency bands verified end to end), an atomic temp-then-move write never fires the watcher because it replaces the inode, and an in-place write fires it into a self-restart after which the proxy accepts TCP connections and immediately closes them (`Empty reply from server`, HTTP 000) and never serves again — while a new process with the byte-identical config works perfectly. Not fixed here: the spines are a finalized upstream artifact owned by `bmad-ux`, so the implementation records the correction in this spec's Design Notes and ADR-019 rather than editing them. The console already states the restart cost in the Faults workspace before the operator pays it."
- source_spec: `docs/bmad/implementation-artifacts/spec-demorunner-fault-injection-workspace.md`
  summary: "`CoreBankDemo.DemoRunner.Tests` intermittently reports a total of one fewer test than usual (369 vs 370, and earlier 347 vs 348) with no failures and `Skipped: 0` in every run — so a test occasionally does not execute, which the build workflow's own Matrix Test Audit treats as equivalent to a missing test."
  evidence: Observed twice across roughly ten runs, by two independent operators, and not reproducible on demand (three consecutive runs after a clean rebuild all reported the higher count). Investigated and ruled out: the suite's only dynamic skip is `CommandRunnerTests.RequirePrintenv`, which is `OperatingSystem.IsWindows()`-guarded and therefore never fires on Linux; the only `TheoryData` source (`AspireAdapterTests.DescribeFailures`) is a static literal; no `Random`, `DateTime.Now` or environment-derived value feeds any test-data source. Cause not identified. Worth pinning before this suite is relied on as a CI gate, since a silently non-executing test is indistinguishable from a passing one in the summary line.
