---
scope: CoreBankDemo full rebuild (initiative altitude)
purpose: build-substrate
altitude: initiative
updated: 2026-08-21T16:31
---

- (constraint) constraints.md binding: external contract frozen, five invariants, conventions skills, test bar 90 percent line on logic projects
- (decision) AD-1 ADOPTED: externally observable contract identical to main (endpoints, ports, topics, CloudEvents, seeded accounts); prevents scope creep during rebuild
- (decision) AD-2 paradigm: hexagonal (ports and adapters) per service over a shared messaging kernel; controllers thin, application logic pure, infrastructure behind ports; prevents logic bleeding into controllers/infra where it cannot be unit tested
- (decision) AD-3 messaging kernel: every processor (both inboxes, both outboxes) derives from shared InboxProcessorBase/OutboxProcessorBase; delivery is a pluggable IOutboxDeliveryStrategy port (HTTP forward / Dapr publish); rules A2 ruling - no processor may re-implement poll/lock/partition/retry
- (decision) A1 ruling: delete Features:UseDapr flag entirely; single HttpCoreBankApiClient behind ICoreBankApiClient port; rationale: flag never worked, Dapr client never existed, YAGNI for demo
- (decision) A3 ruling: PartitionCount=4 everywhere, matching ADR-004 and docs
- (decision) A4 ruling: DELETE LockRenewIntervalSeconds option; keep lock-expiry + cooperative cancellation at 5/6 expiry; rationale: renewal adds concurrency complexity with zero demo value; ADR to be written in docs epic
- (decision) AD-4 identity: idempotency key is the single dedupe+ordering identity; partition = FNV1a(key) mod 4; prevents divergent partition schemes between services
- (decision) AD-5 transactional consistency: ledger mutation, inbox completion, and event enqueue commit in ONE DB transaction; no network I/O inside a DB transaction; prevents lost/phantom events
- (decision) A6 ruling: ports = IDistributedLockService, IEventPublisher (wraps DaprClient), ICoreBankApiClient, repository interfaces, TimeProvider; raw SQL (FOR UPDATE) lives only in repository implementations
- (decision) A7 ruling: test pyramid - pure logic via Moq mocks; repository behavior via EF Core Sqlite in-memory; Postgres-only semantics (FOR UPDATE, unique-violation codes) delegated to k6/Postgres acceptance tier; boundary documented in ADR
- (decision) A8 ruling: coverage gate in tests/Directory.Build.props (coverlet.msbuild Threshold=90 ThresholdType=line); ExcludeFromCodeCoverage on Program/AppHost/DbContext-wiring; gate runs on CoreBankDemo.Rebuild.slnf
- (decision) A5 ruling: ARCHITECTURE.md regenerated from code in final docs epic; until then it describes main, not the rebuild
- (version) verified live on nuget 2026-08-21: xunit.v3 4.0.0, AwesomeAssertions 9.6.0, Moq 4.20.72, coverlet 10.0.1, Microsoft.NET.Test.Sdk 18.9.0, xunit.runner.visualstudio 4.0.0; EFCore.Sqlite pinned 10.0.8 to match solution EF Core 10.0.8
- (assumption) xunit.v3 (not 2.x) chosen as current major; AwesomeAssertions is the maintained FluentAssertions fork per user request
- (decision) AD-6 build gate: CoreBankDemo.Rebuild.slnf strangler; full .sln may be red until AppHost epic completes; prevents fighting red builds mid-rebuild
- (event) Reviewer gate: lint_spine 0 findings; version lens PASS w/ 2 annotations; adversarial lens found 10 permitted incompatibilities (4 critical)
- (change) AD-4 split: ordering identity (idempotency key) vs per-store dedupe identity (event stores use composite TransactionId+EventType+Account) - closes silent event-drop hole
- (change) AD-11 added: delivery outcome contract - Status is transport-only; business rejection = Completed + failure payload, never Failed/retried; HTTP strategy 2xx=Completed else retry to MaxRetryCount; in-flight duplicate gets 202 pending
- (change) AD-12 added: wire contract ownership - event records once in ServiceDefaults; HTTP DTO JSON shapes frozen in epics tech-spec before demolition
- (change) AD-9 tightened: provider-agnostic repositories + minimal excludable FOR UPDATE pass-throughs; VSTest mode mandatory, MTP forbidden (coverlet incompatible)
- (change) AD-3: stale-claim reclaim named kernel-owned; conventions: per-store lock prefixes, single seed-data owner per dataset
- (event) spine finalized
