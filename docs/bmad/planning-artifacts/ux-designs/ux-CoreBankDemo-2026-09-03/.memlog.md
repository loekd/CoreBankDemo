---
topic: DemoRunner reusable operator console
updated: 2026-09-09T19:59
---

- (decision) Replace the slide queue, cue track, slide anchors, speaker notes, and deck-bound scenario as the primary experience with a reusable capability-driven demo operator console.
- (decision) Primary information architecture is topology, resources, operations, and evidence; the two conference talks validate flows but do not define navigation.
- (decision) Use the fast path and mark unresolved interaction choices as assumptions rather than blocking UX drafting.
- (decision) The climax is stopping CoreBankAPI, submitting standard and instant payments, restarting CoreBankAPI, and seeing durable recovery and final assertions without leaving the console.
- (decision) DevProxy delay setup remains a manual external configuration operation; the console may expose status and supported on/off controls but does not edit checked-in proxy configuration.
- (assumption) Keyboard and mouse have full parity, with single-key shortcuts optimized for mid-sentence operation and mouse as the discoverable fallback.
- (assumption) The console may be projected deliberately, so its default view uses stage-readable summaries while raw evidence is available on demand.
- (assumption) The primary layout is one persistent cockpit with switchable workspaces, not a wizard, queue, or deep tree.
- (assumption) Payment operations default to editable presets; generated, supplied, and omitted idempotency modes are explicit, and resend-same-key is a first-class action.
- (assumption) Destructive resource actions require a short hold-to-confirm or typed confirmation shortcut and always keep the inverse recovery action in the same location.
- (assumption) Reset is exposed only for the disposable LoadTests topology through its existing supported reset workflow; the regular topology has no generic reset action.
- (event) Discovery captured from the operator-console brief, existing Story 7.4, ADR-015, instant-payment contracts, current scenario, README, and user corrections; fast-path distillation may begin.
- (event) Draft DESIGN.md and EXPERIENCE.md distilled; surface closure passed for topology, resource control, payments, evidence, load workflow, chaos, and both named talk journeys.
- (change) Corrected Journey A so topology health visibly degrades while CoreBank is stopped and returns healthy only after recovery; the console never claims health stayed green through an intentional outage.
- (decision) Use the supplied Docker Sandboxes screenshot only as composition inspiration: one near-black work surface, compact left navigation, strong verb/object hierarchy, muted command detail, simple top-bar controls, and a fixed lower action/detail region; do not copy its branding or exact styling.
- (decision) A fingerprint-verified attached AppHost permits allow-listed Aspire resource start/stop/restart commands after per-action confirmation; whole-AppHost stop remains forbidden because DemoRunner does not own that process.
- (decision) On relaunch, discard local UI/session history, re-read live Aspire and HTTP state, and start with empty operation history; never restore a prior passed state or cue checkpoint.
- (decision) Destructive actions use a modal with Cancel focused and one explicit confirmation key; keyboard hold and double-Enter confirmation are rejected.
- (change) Renamed the imported visual to operator-console-inspiration.png because it is a generic composition reference; do not attribute its exact appearance or branding to Docker Sandboxes.
- (event) DESIGN.md and EXPERIENCE.md finalized after three-lens validation; remaining documentation findings were resolved and the attached-resource authority was codified in amended ADR-015.
- (override) Overturns the 2026-09-03 decision that DevProxy fault setup stays a manual external operation. The console now owns fault injection as a first-class capability with operator-adjustable error, latency, and throttling levels.
- (decision) Fault levels apply live, mid-run. The console writes a session config and Dev Proxy's built-in file watcher reloads it without restarting the AppHost or the proxy resource.
- (decision) The console never writes the checked-in devproxy config files. It writes a gitignored generated session config; the checked-in files remain untouched and serve as the named default presets, preserving the LoadTests 9500-12000ms profile tuned to overrun the instant rail budget.
- (decision) Chaos controls are added to the existing DemoRunner console spines in Update mode, as a further capability of the same cockpit rather than a separate product.
- (decision) Fault controls get their own fifth workspace, reached by key 5, beside Operations/Resources/Evidence/Load Test. Sliders need vertical room and a fixed action region; placing them inside Resources would force the nested sub-navigation the IA bans.
- (decision) Fault controls are the second named exception to the global single-action-in-flight lock, joining the Burst control's Cancel. Raising latency or error rate while a burst or load run is in flight is the demonstration itself, so the lock must not block it.
- (decision) Fault levels use a stage-then-apply model: dragging a slider has no effect on the running system, and one explicit Apply commits every staged knob at once in a single config write. Staged-versus-live difference must be rendered truthfully.
- (decision) The LoadTests profile exposes all three knobs (error, latency, throttling), with its checked-in 9500-12000ms latency profile loaded as the named default preset. Adding error and throttling plugins to that profile changes what a load run means, so load-test assertions need review.
- (assumption) The console owns the Features:UseDevProxy gate when it starts an AppHost, since the gate is read at AppHost start. On an Attached topology the fault controls are visibly unavailable with the reason stated, matching how the spine already treats Attached.
- (decision) The Faults workspace ships spine-only, with no rendered mockup. It is built from the DESIGN.md component tokens and the EXPERIENCE.md behavioral rules alone.
- (decision) Reviewer Gate: structure and prose lenses only. Rubric-walker coverage and the stage-safety lens were offered and declined for this pass.
- (change) Resolved a pre-existing self-contradiction in DESIGN.md Colors: accent-teal was documented as used exclusively for the primary action and focus ring while the DevProxy chip simultaneously specified teal. Teal now has three explicitly stated roles - primary action, focus ring, and the lock-exempt/faults-in-force signature.
- (assumption) Fault severity is never encoded in color, because the console palette already means proven-pass, proven-fail, and no-proof-yet. Severity is carried by the printed number, bar fill length, and text label.
- (change) Structure and prose review applied: cut a 166-word changelog narrative from DESIGN.md Colors, removed a duplicated colour rule from EXPERIENCE.md Interaction Primitives where it violated the file's behaviour-only ownership, and split an 84-word sentence in the fault-chip specification.
- (event) spines finalized - fault injection capability added to the DemoRunner operator console
- (decision) Add a feedback loop from CoreBank to the DemoRunner console: the console subscribes to the transaction-events broadcast and renders pushed outcomes, closing the gap where a submitted payment sat at 202 Pending until the operator manually ran an outcome query.
- (decision) All three broadcast CloudEvents drive one loop: com.corebank.transaction.completed, com.corebank.transaction.failed, and com.corebank.account.balance.updated. Each type renders appropriately rather than collapsing into a single generic 'processed' signal.
- (decision) Confirmation surfaces in two places only: in place on the originating Operations payment row, which flips Pending to its proven outcome under the operator's finger, and in the Evidence feed, which is where a burst's outcomes accumulate because row-by-row tracking does not scale past a handful of payments.
- (decision) Rejected: a sixth Events workspace, a persistent always-visible event lane, and a full-width stage banner. The IA stays at five workspaces and the feedback loop adds no new surface.
- (decision) Fast path for this update: batch the remaining gaps, draft both spine updates with ASSUMPTION tags on unresolved interaction choices rather than blocking.
- (decision) Correlation is by TransactionId, verified in code rather than assumed: PaymentsController's 202 returns snapshot.TransactionId, and all three CloudEvents carry TransactionId. No new correlation mechanism is invented.
- (decision) The feedback loop gets its own liveness signal, an outcome-feed chip in the topology bar beside the fault chip. Without it the feature can lie on stage: a dead subscription leaves every payment at Pending while the system actually settled everything, and the operator would narrate a failure that did not happen.
- (decision) The feed chip is not teal. Teal already has exactly three roles and the Do's and Don'ts ban a fourth. Listening reuses text-secondary, Not listening reuses text-muted, and Feed lost takes state-failed because a dropped subscription is a proven failure of the console's own observation.
- (decision) Silence is never an outcome. A payment that never settles reads 'Awaiting settlement' with an elapsed readout, never Failed; only a transaction.failed event proves a rejection. This extends the existing ban on treating elapsed time as a proxy for success to its inverse.
- (decision) Burst counters split into an HTTP leg (accepted/failed) and a proven leg (settled/rejected/awaiting). The awaiting number closing live is the visual confirmation for a burst, where row-by-row tracking is useless.
- (decision) The console must never become a competing consumer on transaction-events. Observing must not alter what is observed: diverting events away from PaymentsAPI would break the very demo the loop is meant to prove. Stated as a spine invariant and handed to architecture.
- (decision) Event counts are taken from the producer, not assumed: TransactionExecutionHandler emits TransactionCompleted plus exactly two BalanceUpdated events (debit on FromAccount, credit on ToAccount) for a success, and TransactionFailed alone for a rejection. A rejected payment therefore has no balance legs, and an empty leg column under a Rejected row is correct rather than missing.
- (assumption) The transport carrying the broadcast to the console (SSE, long-poll, or a small read-only relay on an existing service) is an architecture decision. The spine requires only that it be read-only, prompt, liveness-observable, and non-competing with PaymentsAPI's consumption.
- (assumption) Transient emphasis on a settling row is a brief bold/reverse-video attribute change on that row only, never a full-screen flash or a repeating blink, and never longer than about a second. The settled state must be fully readable from a static screenshot taken later.
- (assumption) The Evidence event feed retains the most recent 500 rows and elides older ones behind an explicit count; burst counters are computed from every event received, never only from retained rows.
- (change) Key Flows renumbered: a new Flow 3 (watching two hundred payments prove themselves) joins the canonical capability sequences, pushing Journey A to Flow 4 and Journey B to Flow 5. Flow 1's recovery step now resolves pushed rather than queried, and Journey A's dedupe beat is proven by the absence of a second pair of balance legs.
- (override) Overturns the drafted outcome-feed chip. Loek rejected a seventh topology-bar chip; the topology bar stays as it was. Feed liveness is stated inline instead, parenthesised on an awaiting payment row's own elapsed readout and in the Evidence feed header.
- (decision) The inline treatment is structurally stronger than the rejected chip, not merely smaller. A chip annotates a claim that has gone stale; a row that re-states itself as Outcome unknown withdraws the claim. The console becomes incapable of displaying 'Awaiting settlement' while deaf, rather than merely warning about it elsewhere on screen.
- (decision) A dropped subscription renders state-neutral 'Outcome unknown', never red. state-failed asserts a proven failure, and a lost feed proves nothing about the payment - the money may well have moved. Grey is the accurate answer here, not a softer red.
- (decision) Reviewer Gate: structure and prose lenses only, matching the Faults pass. Stage-safety and rubric-walker were offered and declined.
- (decision) This capability ships spine-only with no rendered mockup, matching the Faults precedent: it adds no new surface, only new states on existing rows, counters, and labels.
- (change) Structure lens applied: 21 findings resolved. Four ownership leaks pulled back out of DESIGN.md (behaviour written into Colors, Burst control, Event row, and four Do's and Don'ts rows); the IA rejection argument merged into Inspiration and Anti-patterns; four of six new Interaction Primitives bullets cut as restatements; the four new Payment state rows regrouped with the existing Payment block; a verbatim doubled clause in the Evidence strip row removed; dead feed-status and orphan burst-counter tokens dropped; settlement-leg-column widened from 20ch to 28ch, which was narrower than its own documented example.
- (change) Prose lens applied. Four substantive corrections beyond copy-editing: 'deaf' used metaphorically in two places violated bias-free language and became 'not listening'; 'faults are armed' was wrong where 'in force' was meant, since armed means every knob is zero and injects no latency; an undefined '202 Accepted' label became the console's defined '202 Pending'; and the Unattributed state cited events from before the subscription, contradicting Foundation's no-replay contract, now a late outcome for an earlier transaction. Also fixed: burst figures read as HTTP status codes and were reversed to label-first.
- (event) spines finalized - outcome feedback loop added to the DemoRunner operator console
- (override) Overturns the drafted transport boundary. The console now subscribes to transaction-events directly through a Dapr sidecar it launches and owns, rather than through a read-only HTTP endpoint on an existing service. User decision during implementation planning.
- (decision) The competing-consumer concern was mechanism confusion on my part, not a real risk. Dapr's Redis pub/sub derives its consumer group from the app-id and PaymentsAPI's subscription is scoped to payments-api, so a console with its own app-id receives a fan-out copy while PaymentsAPI keeps receiving every event. The invariant that observation must not divert an event survives; only the stated mechanism changed.
- (decision) Direct Dapr consumption is better than the rejected polling design on an axis the spine already cared about: it adds nothing at all to the banking services, where polling would have required a new endpoint on PaymentsAPI and thus tripped the spine's own anti-pattern about a demo tool that requires the demonstrated system to be modified for its benefit.
- (decision) Streaming subscription via Dapr.Messaging 1.18.5, verified restorable. The console dials out to its own sidecar over gRPC, so it hosts no inbound HTTP listener and opens no port - preserving more of ADR-015's standalone character than a Dapr.AspNetCore endpoint subscription would.
- (decision) The console spawns and owns its own daprd child process, reusing the existing process adapter and terminator. This keeps the feed working on Attach, where the console did not start the topology and the topology exposes nothing for it.
- (decision) ADR-015 is amended in place rather than superseded by a new ADR: its line 24 socket clause and package-reference sentence become false under this decision. The machine-checkable invariant it protects - zero banking implementation project references - is unaffected, because Dapr.Messaging is a package reference and the three event contracts are declared as local wire types.
- (override) Overturns DESIGN.md's assumption that no dark/light split would be modelled. The console now ships two palettes. Dark stays the default and the brand identity; light exists because the near-black canvas washes out under a projector lamp, and projector legibility is the harder requirement the spine already committed to.
- (decision) Light mode carries its own hexes rather than the dark set on a pale background, and this was measured rather than argued. Every dark token as a foreground on white: state-running 2.17:1, state-healthy 2.37:1, accent-teal 2.48:1, text-secondary 2.44:1, text-muted 3.43:1, state-failed 3.96:1, text-primary 1.19:1. Eight of ten fail AA.
- (decision) The light palette is anchored to the Ghostty GitHub Light Default theme the talk is presented in - its exact background #FFFFFF and foreground #1F2328, with state and accent hues from the same theme's Primer ANSI set. The console therefore does not read as a foreign rectangle inside its terminal, and the 16-colour fallback degrades to the same colours rather than approximations.
- (decision) Every light state and accent token clears 6:1 on the canvas, above the 4.5:1 AA floor, because the reading that matters is from the back of a room. Amber is darkened hardest, to #7A5309, since it is the worst-behaved hue on white; its identity survives because the text label was always the primary discriminator for that state, never the colour.
- (change) accent-navy #325F8C is the single token identical in both modes, clearing the floor from both directions (5.62:1 against dark text, 6.66:1 against white). Keeping one fixed structural accent is deliberate: it says which section you are in, and that meaning should not shift with the lighting in the room.
- (change) state-failed-on-raised collapses onto state-failed in light mode. The dark palette needs a lightened tint because its base red measures a sub-AA 4.12:1 on surface-raised; the light red measures 6.75:1 there, so a separate tint would be decoration. The token is retained so both palettes fill the same shape.
- (decision) Theme selection is a --light/--dark flag plus COREBANK_DEMO_THEME, with a live T toggle from any workspace. Loek chose the live toggle over flag-only: a projector that washes the dark canvas out is discovered during the talk, not before it. Auto-detection via OSC 11 was offered and not taken.
- (decision) The T toggle is a repaint and nothing else - no focus move, no scroll, no workspace change, no effect on staged or applied fault levels, no interruption of an action in flight. It is exempt from the single-action-in-flight lock by nature rather than by exception, and takes no lock-exempt accent-teal signature because it is not a control on the surface.
- (change) Resolved spine-versus-code drift the light mode surfaced: DESIGN.md specified an accent-navy fill for the active nav-rail item, but the code marks it with a leading marker glyph and no fill. The built behaviour is the better one - colour-independent, and it survives a monochrome terminal - so the spine was aligned to the code rather than the reverse. The marker now carries the active state and any fill is reinforcement.
- (change) nav-rail-item's single foreground token split into foreground-active and foreground-inactive, and a text-on-accent token added to both palettes. accent-navy deliberately does not invert between modes while text-primary does, so the original straight token swap would have put dark text on mid-navy at 2.37:1 in light mode - the one component in the spine that a palette swap alone could not carry.
- (assumption) Ghostty's GitHub Light Default background is taken as #FFFFFF from the published iTerm2-Color-Schemes definition rather than sampled from Loek's own terminal. It matters less than it looks: Terminal.Gui paints explicit background hexes, so contrast is measured against the canvas the console paints, not the one the terminal supplies. The theme value only governs how well the console blends at its own edges.
- (event) spines updated - light mode added to the DemoRunner operator console
- (change) Fixed a layout defect the spine already forbade: the Operations payment list rendered zero rows at the documented 80x24 minimum and one row at the 100x30 preferred baseline, so a submitted payment appeared in Evidence and nowhere in Operations. The row ladder used fixed constants that counted rows the workspace did not have, ignoring the FrameView border and the rows Dim.Fill reserves. Positions are now derived from the real available height, the list is budgeted first, and its floor is asserted at every supported terminal size.
- (decision) Degradation order for the Operations form is now explicit: the Omitted-mode warning gives up its row first and only appears in Omitted mode, then the blank separator rows, then the workspace hint. The feed status is never surrendered, because an unresolved payment must never look awaited while nothing is listening. No control is ever hidden and the outcome lookup always stays above the list.
- (decision) Update pass opened: Loek is unhappy with the Operations workspace, wants a cancel affordance for a running instant payment, and wants currency removed as an input. Stated preference: visible separation (hairline rules) between the single-payment area, the burst area, and the outcome list, which today read as one undifferentiated surface.
- (decision) Operations workspace adopts the Stage-focus layout: a one-line compose bar at the top, one large focus card for the payment currently happening (state, elapsed, lifecycle in words, and its action), and a compact EARLIER list beneath. Chosen over ruled-bands and two-column because the screen is read from the back of a 250-seat room and one payment must be legible there.
- (decision) Cancel acts on the selected payment with no confirmation modal. Reconciled with the Stage-focus choice: the focus card renders the selected payment, selection defaults to the newest unresolved one and follows each new submission, so 'the selected row' and 'the big card' are the same object and Cancel needs no separate target.
- (change) Burst counter lines renamed from 'HTTP leg' and 'Proven leg' to 'Sent' and 'Settled'. The leg vocabulary named the plumbing and failed the read-aloud test on its own author. Also retiring 'Outcome key' as a label: the manual lookup becomes an action on the selected payment rather than a standing text field.
- (decision) Currency is removed as an operator input. It stays required on the wire, so the console sends a fixed EUR and the field plus its three-uppercase-letter validation rule are deleted. Assumption pending confirmation that every demo account is EUR.
- (override) Overturns the persistent Evidence strip and status band as shell chrome. All three bottom rows are removed from every workspace, not only Operations, so the shell stays uniform. A failed or refused command announces itself as a transient overlay line at the foot of the content and then clears. To keep this from violating the spine's own rule that silence is never an outcome, the transient is an announcement only: the durable record of every action stays in the Evidence workspace, and no state is ever carried by the transient alone.
- (decision) A running burst takes over the entire Operations content area, replacing the compose bar, focus card and EARLIER list until it drains or is stopped. Chosen over a burst-shaped focus card because the counters are the demonstration during a burst and deserve the whole surface. On drain the takeover holds its final Sent/Settled summary until dismissed, so the room can read the result rather than watching it vanish.
- (decision) The compose bar carries a Generated/Supplied/Omitted chip on its single line. The supplied-key field appears as a second line only in Supplied mode and the not-retry-safe warning only in Omitted mode, so no control in use is ever hidden and the dedupe demo stays one keystroke deep.
- (decision) Grounded critique of the shipped Operations screen that drove this pass: ten of thirty rows are shell chrome before any content, the form occupies rows 0-15 and the payment list starts at row 17, three unrelated activities share one flat column with blank rows as the only separator, and the labels HTTP leg, Proven leg, Outcome key and Omitted mode name the plumbing rather than the meaning. Loek could not parse three of his own labels, which is the finding.
- (decision) Cancel is offered on any payment that has no proven outcome yet, not only instant-rail ones. CoreBank's cancel endpoint is rail-agnostic, and hiding the button on a slow standard payment would read as a missing capability rather than a design choice.
- (decision) The compose bar keeps its field captions and takes two lines: From and To on the first, Amount, Rail and Key on the second. Submit anchors at the right edge of the first line and Burst at the right edge of the second. Two eighteen-character IBANs plus captions do not fit in the 74 usable columns of a single line, which is what forced the choice.
- (override) Overturns the spine rule that each workspace's one primary action anchors in a fixed lower action/detail region. In the Stage-focus Operations layout Submit sits in the compose bar it belongs to, and the payment's own action (Cancel payment, Look up outcome, Resend same key) sits on the focus card it acts on. Actions now anchor to the object they operate on, which the removal of the bottom band makes both possible and necessary. The rule still holds for Resources, Faults and Load Test.
- (decision) The EARLIER list truncates account identifiers to the last four digits. The seeded demo accounts are eighteen-character IBANs and two of them plus an amount and an outcome cannot share a 74-column row. The full identity is never lost: selecting the row moves it into the focus card, which prints both accounts in full.
- (decision) Selection drives the focus card. The card renders the selected payment, selection defaults to the newest unresolved one and follows each new submission, so the operator can cancel or look up a payment without a separate target control.
- (change) Currency assumption confirmed rather than assumed: DemoAccountSeeder inserts exactly three demo accounts, NL91ABNA0417164300, NL20INGB0001234567 and NL39RABO0300065264, all EUR. The console can send a fixed EUR with no risk of a currency mismatch, and the removed input costs nothing.
- (change) The EARLIER history list is cut from Operations and replaced by a STILL OPEN strip that lists only payments with no proven outcome, collapsing to zero rows when nothing is open. Loek challenged whether the list earned its space given the Evidence workspace already holds history, and he was right about history but not about concurrency: Flow 1's climax has a standard and an instant payment in flight at once, and one focus card cannot show both. Separating unfinished business from history keeps that beat on the submission screen and costs nothing at rest.
- (decision) Cancel feasibility verified in code, not assumed. EndpointResolver already sends the outcome query straight to CoreBankAPI at 127.0.0.1:5032, so POST /api/transactions/cancel is the same base URL and the same pattern, adding no endpoint to any banking service and honouring the rule that the demo tool consumes the system as-is. CoreBank's TransactionRequest needs FromAccount, ToAccount, Amount, Currency and TransactionId, and the console holds all five: the first four from the submitted form and the fifth from the 202 response, which is the same TransactionId every outcome CloudEvent carries. The cancellation resolves through the existing feed because CoreBank now publishes com.corebank.transaction.cancelled.
- (assumption) The navigation rail is 22 columns wide for five labels whose longest is Load Test. With the one-character active marker and the shortcut digit it needs 14 plus its 2 border columns, so 16 suffices and 6 columns return to the workspace. Proposed as part of this pass because the compose bar is the region those columns buy: two 18-character IBANs with captions currently leave no slack. Flagged rather than decided, since the rail is shell chrome shared by all five workspaces.
- (decision) Confirmed: the navigation rail shrinks from 22 columns to 16 in its preferred state, the compact 5-column state is unchanged. Six columns return to every workspace, not only Operations. At a 100-column terminal the workspace inner width goes from 74 to 80, which is what gives the compose bar its slack. Recomputing the 80-column floor with the compact rail shows 71 usable inner columns rather than the 51 the first wireframes assumed, so the hard minimum is far less cramped than drawn.
- (decision) Reviewer Gate for this pass: stage-safety and structure lenses. Stage safety was declined on both prior passes and is taken this time because the change removes the evidence band that every workspace used to announce outcomes, and adds a cancel action with three outcomes the console must never confuse. Rubric walker declined again. Prose is not run as a gate lens but still applies at polish through the configured document standards, so structure runs once here and prose once at polish rather than both twice.
- (event) Both spines distilled against the update brief. DESIGN.md added twelve spacing tokens and six components and deleted the evidence strip, adding no colour token in either palette because a cancellation reuses state-neutral by the same argument the Outcome unknown rule already makes. EXPERIENCE.md took the fourth CloudEvent type into Foundation, rewrote the Operations IA, Component Patterns, State Patterns, Accessibility, Responsive and three Key Flows.
- (change) Cross-file check found exactly one new pairing break: DESIGN.md specified a still-open-strip component while EXPERIENCE.md deliberately left it out to avoid an unpaired row, so the row was written and inserted after Focus card. Three other name mismatches were confirmed pre-existing by re-running the check against the committed files, and were left alone. DESIGN.md token references and both palettes validate clean.
- (decision) Version-control check requested by Loek before any build: both stories from 2026-09-08, commits 73ccbb1 and 5daf0f4, are already contained in origin/main, confirmed by a live ls-remote rather than a cached ref. No pull request was ever opened for them; the most recent is #13 from 2026-09-08. They reached main directly, which departs from the project's stated policy that every change lands through a pull request. This UX branch was cut from origin/main at 5daf0f4 and therefore already sits on top of both.
- (change) Correction, and it was Loek's: I claimed the console could not cancel inside the instant rail's budget because CoreBank identifies a payment by a server-generated TransactionId. PaymentStorageHandler actually sets IdempotencyKey and TransactionId to the same value, generating one only when the client omits the header. In Generated and Supplied modes the console therefore holds the transaction id before it sends the request, and Cancel is live for the whole budget window. Only Omitted mode must wait for the response, which is a coherent further consequence of the mode already labelled not retry-safe.
- (decision) The focus card appears the moment Submit is pressed, not when the response returns, carrying the request, a running clock and a live Cancel. This resolves the stage-safety finding that a submission whose response never arrives leaves nothing on screen, and it is what makes cancelling a running instant payment reachable at all. In Omitted mode Cancel renders disabled with its reason stated, because the console has no transaction id until the bank answers.
- (decision) Verified rather than assumed: the console's refusal paths return through RejectedPayment without writing an Evidence entry, so today those messages live only on the message line this pass removes. Removing the bottom band is therefore conditional on a stated spine requirement that every refusal the console produces itself is recorded in the Evidence workspace before the transient announces it. Without that the transient would be the only record, which the pass's own rule forbids.
- (change) Stage-safety and structure lenses both returned Fail: 4 critical, 8 high, 13 medium and 9 low across the two, 41 findings. All were accepted; none was rejected. EXPERIENCE.md remediation is complete and verified — section order intact, the 504 vocabulary now present in eight places, the three-CloudEvent claim gone. The reviewers disagreed twice and both were reconciled: a 504-cancelled payment leaves the still-open strip when its answer lands rather than never entering it, and the Sent line never re-bases its denominator while the burst bar does on a stopped burst.
- (change) Wireframes extended to fourteen screens on the review's findings: a submitted-no-answer-yet card with a live Cancel, the same card in Omitted mode with Cancel disabled and its reason on screen, and a rail-initiated withdrawal at nine seconds reading as cancelled by the rail with its 504, which the spines had previously described as a payment that simply never resolves. Both burst screens now carry the feed statement on their captioned rule.
- (change) Prose lens applied: ten substantive corrections beyond copy-editing, four over-long sentences split, six copy-edits. The substantive ones include a 504 withdrawal wrongly called a rejection, which collides with Rejected as a defined red state word; a survival of the 409 finding in DESIGN.md still asserting live execution; two files specifying two different burst bar captions; and amber still listed as covering unknown, which state-neutral owns by two separate arguments in the same file.
- (change) Three wireframe-versus-spine conflicts the prose lens surfaced, all introduced when the new screens were drawn, all resolved toward the smaller vocabulary. The in-flight card uses the existing awaiting tilde and the words NO ANSWER YET rather than a new ellipsis symbol with no monochrome fallback. The disabled cancel keeps the same label dimmed rather than a longer one that overran the action slot the token derivation is built on. The still-open strip carries no per-line action; the remedy is one selection away on the card, which removes a contradiction where one file offered the action on the line and the other gave its rows no slot for it.
- (decision) A rail-initiated withdrawal gets its own card state word, CANCELLED BY THE RAIL, distinct from the operator-fired CANCELLED. Loek chose this over one shared word with the provenance on the line below. The symbol and colour stay identical because the outcome for the money is identical; only the word differs, so the room does not need the presenter to say which of the two just happened. It is the seventh and longest card state word and sets the 34-cell state column. The drawn screen was right and both spines were the side that changed.
- (change) Two corrections to this log, both caught by the report synthesis rather than by me. The severity split recorded earlier as 4 critical, 8 high, 13 medium and 9 low sums to 34, not the 41 findings stated alongside it; the counts taken from the review files themselves are 5 critical, 14 high, 13 medium and 9 low. And the claim that the stage-safety lens had been declined on both prior passes is false: a stage-safety review of these spines was run on 2026-09-03 and this pass supersedes it, re-checking its ten findings. Only the rubric walker has been declined on every pass.
- (event) Validation report rendered in both formats. All eight rubric categories are marked not assessed rather than guessed, because the rubric walker was declined and the review-rubric.md on disk describes the pre-Stage-focus spines. No accessibility lens ran this pass either, and the report says so. The wireframes moved from .working to wireframes during the run, so both review files were repointed.
