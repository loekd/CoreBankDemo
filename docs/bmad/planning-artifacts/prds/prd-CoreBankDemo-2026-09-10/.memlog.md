---
topic: Evidence payload bodies (request/response/message JSON on the Evidence screen)
updated: 2026-09-10T13:22
---

- (decision) Goal: a Postman/.http-style request+response readout on the Evidence screen; the money shot is showing the transaction id IS the Idempotency-Key, and that an idempotent retry returns the identical response.
- (decision) 'Messages' means CloudEvents off transaction-events; the console should show the event as delivered, not a summary of it.
- (decision) Audience is Loek plus a live audience; the demo beats are retry/idempotency and message-based interaction.
- (decision) Scope constraint (Loek, verbatim intent): keep modifications to a minimum and keep the console's interpretation to a minimum -- he narrates meaning on stage, the console shows bytes.
- (change) Two UI asks bundled into this run: Details pane gets a border and higher contrast on the same background colour as the rest; Evidence list rows drop everything after the em dash.
- (decision) Not every evidence row has a payload (e.g. the Aspire 'started Regular as Owned' topology row); 'where applicable' is a real per-kind decision, not a blanket add.
- (event) Discovery: brain dump captured.
- (decision) Working mode: Fast path -- draft the full PRD now, [ASSUMPTION]-tagged where inferred.
- (decision) Scope confirmed as three captures: (1) outbound requests, (2) inbound CloudEvents held in memory, (3) responses (already captured).
- (decision) Redaction is dropped entirely -- Loek: 'totally useless' in a demo over fake data. Consequence accepted: exported sessions carry raw headers and bodies.
- (decision) Details pane layout: side-by-side REQUEST | RESPONSE columns (Postman's layout), chosen over stacked sections and tabs.
- (event) Fast-path draft written: prd.md (8 feature groups, FR-1..FR-24) and addendum.md (capture sites, rendering options, rejected layouts).
- (decision) Redaction: secret substitution removed; the 8192-char bound is kept (confirmed by Loek). JournalRedaction should be renamed since it no longer redacts.
- (decision) Response headers are in, rendered Postman-style as a name/value list above the body, on HTTP-exchange records.
- (decision) Copy yields raw request/response text rather than the column layout (confirmed).
- (decision) Narrow-terminal stacked fallback dropped in favour of Auto scroll bars on both axes -- Loek presents at HD or better. Verified Terminal.Gui 2.4.17 exposes View.VerticalScrollBar/HorizontalScrollBar with ScrollBarVisibilityMode.Auto.
- (change) PRD renumbered to FR-1..FR-25; open questions reduced to two (unparseable CloudEvents, CloudEvent envelope completeness in Dapr.Messaging TopicMessage).
- (decision) CloudEvent envelope: verified Dapr.Messaging 1.18.5 TopicMessage exposes Data/DataContentType/Extensions/Id/Path/PubSubName/Source/SpecVersion/Topic/Type -- no subject, no time. FR-6 corrected; open question closed.
- (decision) Unknown CloudEvent types now get an evidence row (FR-8): envelope + raw data, no typed interpretation, no transaction id, attributed to nothing so it stays out of burst/tracked-payment accounting. Renumbered FR-8..FR-26.
