apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: otel-config
spec:
  tracing:
    samplingRate: "1"
    otel:
      endpointAddress: "localhost:4317"
      isSecure: false
      protocol: grpc
  metrics:
    enabled: true
