bin/
obj/
/packages/
riderModule.iml
/_ReSharper.Caches/

# SQLite databases
*.db
*.db-shm
*.db-wal

/.idea
.DS_Store

# BMAD personal (user-scope) config
_bmad/config.user.toml

# Coverlet output (tests/Directory.Build.props gate)
tests/**/coverage.json
dotnet/runfile-discovery/CoreBankDemo-28ffb55a5460842cf50353e2d7ceb84e595d4bd536a9f289175b85dab0fde9ba/cache.staging.json

# Story 7.4 / ADR-015: DemoRunner's local run artifacts (journals, captured
# output, rehearsal proof packs) — generated at runtime, never checked in.
/.demo-runner-artifacts/
/.demo-runner-exports/

# Dev Proxy generates its root certificate on first start
CoreBankDemo.AppHost/dev-proxy/

# spec-demorunner-fault-injection-workspace: DemoRunner's Faults workspace writes a
# per-session Dev Proxy config beside each AppHost's checked-in profile (which stays a
# read-only preset source). Generated per session, never checked in.
*/devproxy/config/generated/
